#One_replication document includes 1 R code:
    #CLIME.R outputs the CLIME estimator and the running time under both Gaussian design and sub-Gaussian design under one replication  
      and saves the result.      
#Combine document includes 1 R code that compute the support recovery performance and matrix norm losses and combine the results under 100 replications based on the preserved Rdata using code in One_replication document.

